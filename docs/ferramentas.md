# Ferramentas

MTE: Certidão de Débitos Trabalhistas expõe 1 ferramenta (todas somente leitura).

### 1. `mte_certidao_debitos_consultar`
**Input**: `cnpj` (opcional), `cpf` (opcional), `login_cpf`, `login_senha`, `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

MTE: Certidão de Débitos Trabalhistas, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar MTE: Certidão de Débitos Trabalhistas
```
